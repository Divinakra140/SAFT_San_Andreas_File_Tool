<Project>

  <!-- See Directory.Build.targets for the "._*" AppleDouble sidecar file exclusion — it has to
       live there, not here, since .props is imported before the SDK's default item globs exist. -->

</Project>
